# Write multiple experiments here, under is a baseline example.
# You can add as many experiments as you like, and they will be run in sequence.

experiments:

  - name: "baseline"
    train_mode: "baseline"
    feature_type: "mfcc"